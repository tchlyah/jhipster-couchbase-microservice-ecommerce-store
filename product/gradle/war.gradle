apply plugin: "war"

bootWar {
    mainClass = "com.jhipster.demo.product.ProductApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
